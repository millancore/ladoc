#!/usr/bin/env php
<?php

$rootPath = dirname(__DIR__);
$LadocVersion = 'v0.0.7';

require $rootPath . '/bootstrap.php';

use Ladoc\Mcp\Server;
use Ladoc\Mcp\ToolHandler;

(new Server(
    new ToolHandler($rootPath . '/bin/ladoc'),
    $LadocVersion
))->run();
